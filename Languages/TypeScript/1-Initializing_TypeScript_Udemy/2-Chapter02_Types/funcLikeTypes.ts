let aula23: string = 'Class about Functions like types';
console.log("\n\n" + aula23);

//Using arrow functions

let calculo: (A: number, B: number) => number;

calculo = multiplicar;
console.log(calculo(5, 6));
